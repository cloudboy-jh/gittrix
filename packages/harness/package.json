{
  "name": "@gittrix/harness",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "bun test --pass-with-no-tests",
    "run": "bun src/runner.ts"
  },
  "dependencies": {
    "@gittrix/adapter-cloudflare-artifacts": "workspace:*",
    "@gittrix/adapter-local": "workspace:*",
    "@gittrix/core": "workspace:*"
  }
}
